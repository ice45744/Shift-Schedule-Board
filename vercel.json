{
  "framework": null,
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm -w run typecheck:libs && (cd artifacts/duty-scheduler && BASE_PATH=/ pnpm run build -- --outDir ../../public)",
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/server" }
  ]
}
