{
  "framework": null,
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm -w run typecheck:libs && BUILD_OUTDIR=$(pwd)/public BASE_PATH=/ pnpm --filter @workspace/duty-scheduler run build",
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/server" }
  ]
}
