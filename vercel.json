{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm -w run typecheck:libs && BASE_PATH=/ pnpm --filter @workspace/duty-scheduler run build",
  "outputDirectory": "artifacts/duty-scheduler/dist/public",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/server" }
  ]
}
