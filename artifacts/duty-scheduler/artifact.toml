kind = "web"
previewPath = "/"
title = "ตารางเวร"
version = "1.0.0"
id = "artifacts/duty-scheduler"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20404

[services.development]
run = "pnpm --filter @workspace/duty-scheduler run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/duty-scheduler", "run", "build" ]
publicDir = "artifacts/duty-scheduler/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20404"
BASE_PATH = "/"
