kind = "mobile"
previewPath = "/mobile"
title = "ตารางเวร Mobile"
version = "1.0.0"
id = "artifacts/duty-scheduler-mobile"
router = "path"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
name = "expo"
paths = [ "/mobile" ]
localPort = 20405

[services.development]
run = "pnpm --filter @workspace/duty-scheduler-mobile run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/duty-scheduler-mobile", "run", "build" ]
run = "pnpm --filter @workspace/duty-scheduler-mobile run serve"

[services.env]
PORT = "20405"
